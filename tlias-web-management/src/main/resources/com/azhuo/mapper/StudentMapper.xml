<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--解释foreach标签
    collection: 集合名称，这里是 exprList
    item: 集合中的每个元素，这里是 expr
    separator: 每个元素之间的分隔符，这里是逗号
-->

<mapper namespace="com.azhuo.mapper.StudentMapper">
    <!-- 分页查询学员 -->
    <select id="page" resultType="com.azhuo.pojo.Student">
        SELECT s.id, s.name, s.no, s.gender, s.phone, s.id_card, s.is_college, s.address, s.degree, s.graduation_date, s.clazz_id, s.violation_count, s.violation_score, s.create_time, s.update_time,
               c.name
        FROM student AS s
        LEFT JOIN clazz AS c
        ON s.clazz_id = c.id
        <where>
            <if test="name != null and name != ''">
                s.name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="degree != null">
                AND s.degree = #{degree}
            </if>
            <if test="clazzId != null">
                AND s.clazz_id = #{clazzId}
            </if>
        </where>
        ORDER BY s.update_time DESC
    </select>
    <!-- 添加学员 -->
    <insert id="insert" parameterType="com.azhuo.pojo.Student">
        INSERT INTO student
            (name, no, gender, phone, id_card, is_college, address, degree, graduation_date, clazz_id, create_time, update_time)
        VALUES
            (#{name}, #{no}, #{gender}, #{phone}, #{idCard}, #{isCollege}, #{address}, #{degree}, #{graduationDate}, #{clazzId}, #{createTime}, #{updateTime})
    </insert>
    <!-- 根据ID查询学生 -->
    <select id="selectById" resultType="com.azhuo.pojo.Student">
        SELECT id, name, no, gender, phone, id_card, is_college, address, degree, graduation_date, clazz_id, violation_count, violation_score, create_time, update_time
        FROM student WHERE id = #{id}
    </select>
    <!-- 批量删除学生 -->
    <delete id="delete" parameterType="java.util.List">
        DELETE FROM student WHERE id IN
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>
    <!-- 修改学生 -->
    <update id="update" parameterType="com.azhuo.pojo.Student">
        UPDATE student SET
            name = #{name},
            no = #{no},
            gender = #{gender},
            phone = #{phone},
            id_card = #{idCard},
            is_college = #{isCollege},
            address = #{address},
            degree = #{degree},
            graduation_date = #{graduationDate},
            clazz_id = #{clazzId},
            violation_count = #{violationCount},
            violation_score = #{violationScore},
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>


    <!-- 统计班级人数 -->
    <select id="countStudentCount" resultType="java.util.Map">
        SELECT
            c.name AS clazz_name,
            COUNT(s.id) AS num
        FROM clazz AS c
                 LEFT JOIN student AS s ON c.id = s.clazz_id
        GROUP BY c.id
        ORDER BY num DESC
    </select>

    <!-- 统计学员学历 -->
    <select id="countStudentDegreeData" resultType="java.util.Map">
        SELECT
            (CASE
                 WHEN degree = 1 THEN '初中'
                 WHEN degree = 2 THEN '高中'
                 WHEN degree = 3 THEN '大专'
                 WHEN degree = 4 THEN '本科'
                 WHEN degree = 5 THEN '硕士'
                 WHEN degree = 6 THEN '博士'
                END) AS name,
            COUNT(*) AS value
        FROM student
        GROUP BY degree
        ORDER BY value DESC
    </select>
</mapper>